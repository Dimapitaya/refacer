# CLAUDE.md — refacer

Точка входа для Claude Code в репозитории `refacer`.

- Сначала прочитать `AGENTS.md`, затем `.ai/PROJECT_CONTEXT.md`, `.ai/SAFETY_RULES.md`, `.ai/AGENT_ROUTING.md`.
- Стиль инструкций можно брать из репозитория Dmitrii (`AGENTS.md` / `CLAUDE.md`), НО не импортировать
  сюда задачи Dmitrii и не путать репозитории.
- Handoff-файлы из других веток/чатов (включая Dmitrii handoff) — это только контекст-границы,
  а не список задач. Действовать по ним можно лишь при явной прямой команде Дмитрия.
- Работать на русском языке.
- Не менять `main` напрямую; работать через отдельные ветки; merge/push — только с разрешения.
- Не запускать refacer, не ставить зависимости, не качать модели, не обрабатывать изображения
  без явного разрешения.

(Agent Supervisor — вне зоны ответственности этого репозитория; упоминается только как граница.)
